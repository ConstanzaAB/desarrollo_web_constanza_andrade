# Tarea 1
Uso de HTML5 Y CSS3
## Desiciones tomadas
Para la validación se realizo lo siguiente:
* Si al enviar el formulario alguno o varios de los campos obligatorios estan vacíos, no se deja enviar el formulario y se regresa a este. Se mnatienen los datos llenados y se marcan en rojo los campos que faltaron por llenar o por agregar (archivo).